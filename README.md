# Java GUI with MySQL Database

This is a simple Java GUI application that connects to a MySQL database. The application allows you to add, update, delete and view data from the database. The application uses the JDBC driver to connect to the MySQL database.